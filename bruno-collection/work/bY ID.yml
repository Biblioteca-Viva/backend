info:
  name: bY ID
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_url}}work/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: 22222222-2222-2222-2222-222222222001
  scripts:
    - type: after-response
      code: bru.setGlobalEnvVar("work_id", res.getBody().id)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
