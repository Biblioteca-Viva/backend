info:
  name: Get By Type
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_url}}work?type=art"
  params:
    - name: type
      value: art
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
